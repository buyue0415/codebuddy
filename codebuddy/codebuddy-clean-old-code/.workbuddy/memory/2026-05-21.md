# 2026-05-21 工作记录

## 手续费数据一致性问题修复
- **根因**：parse_statement.py 构建 closed_positions 时漏掉 trades 和 total_other_fees 字段
- **修复**：从 broker_statement.json 的 all_trades 重建已清仓股票 trades，补充 total_other_fees
- **HTML修复**：
  - 持仓总览 totalFees 现在包含已清仓股票（之前只统计当前持仓 100.68）
  - 手续费分析 `o=0` 硬编码 → `o=p.total_other_fees||0`
  - 新增"费用合计"卡片
  - 两边统一为 220.26（佣金143.51 + 印花税60.67 + 其他16.08）
- 附带修复：`</sub>` → `</div>` 标签闭合错误

## 日预测专题模块（第8页）
- **页面结构**：
  - 股票切换（兴业/招行）+ 历史日期选择器
  - 次日预测卡片（方向/信心/区间/操作建议）
  - 4时段逐小时预测卡片（开盘段/午前段/午后段/尾盘段，每段方向+区间+强度+备注）
  - 日内走势预测图（Chart.js line：实际走势+预测区间+前收参考线）
  - 7信号仪表盘（MACD/RSI/布林/KDJ/季节性/ATR/资金）+ 雷达图
  - 自学习状态面板：预测准确率 + 信号可信度权重条
  - 近20日历史命中率点阵图
- **数据结构**：
  - daily_predictions：每条含 date/code/prev_close/next_day/hourly/signals/actual
  - learning_params：signal_weights(7信号×5时段)/hourly_bias/seasonal_adj/ema_alpha
  - accuracy_stats：last_20 + last_60 方向/区间/分时段命中率
- **信号计算**：从月度K线数据计算 ATR(14)/RSI(14)/MACD(EMA12-EMA26)/布林带/KDJ/季节性因子/资金流向
- **生成脚本**：scripts/gen_prediction_data.py
- **示例数据**：2026-05-21 兴业看跌(74%)、招行看跌(66%)

## 定时任务调整
- **08:30 新任务** `银行股日预测生成（08:30）`（automation-1779328367206）：各交易日 8:30 运行，计算信号+生成当日预测+注入HTML
- **15:30 任务更新** `银行股系统每日数据更新+预测回填`（automation-1779269119201）：追加预测回填（actual字段）+自学习（更新权重/偏移/准确率）
- **每周一9:00** `银行股专家周度分析`（automation-1779270872409）：不变

## 日K线数据替换
- **数据源**：westock-data kline --period day --fq qfq（腾讯自选股接口）
- **拉取**：兴业/招行各200条日K线（约1年数据），含 date/open/close/high/low
- **存储**：system_data.json 新增 `kline_daily` 字段
- **信号计算精度提升**：
  - 兴业 ATR: 月线0.369 → 日线0.176（降低50%+）
  - 招行 ATR: 月线0.631 → 日线0.379
- **08:30任务已更新**：改用 westock-data 拉日K + 行情用 neodata-financial-search
- **构建脚本**：scripts/build_daily_kline.py（自动拉取+计算+生成预测）

## 自学习系统完整升级（MWU+EG+Beta）
- **参考学术方案**：
  - Stanford CS168 Hedge/MWU 乘性权重更新 → 信号权重
  - CMU Regret Minimization Exponential Gradient → 偏移量优化
  - Stanford TS Tutorial Beta-Binomial → 信心度校准
  - 滚动EMA → 季节因子更新
- **新学习参数结构**（scripts/upgrade_learning.py）：
  - `signal_weights`：7信号×5时段 MWU初始权重=1.0，β衰减0.7，每轮归一化
  - `hourly_bias`：5时段 EG偏移，η=0.01×(0.995)^n衰减，约束[-0.05,+0.05]
  - `confidence_beta`：3方向 Beta(α,β) 后验，α=命中次数+1, β=失误次数+1
  - `seasonal_adj`：12个月 EMA(0.2) 滚动更新
  - `mw_beta`: 0.7, `learning_rate`: 0.01, `update_count` 累计
- **15:30任务已更新**：完整四维自学习（MWU归一化+EG梯度+季节EMA+Beta分布）
- **HTML自学习面板已更新**：4卡片（准确率+Beta置信度+MWU权重条+EG偏移指示器）
- **预测生成已升级**：使用加权信号方向+时段偏移+季节修正+Beta后验信心度

## 系统清理与自动化补全
- **数据清理**（scripts/cleanup_data.py）：
  - 移除 quotes 中非持仓4只股（600900/601939/601398/600050）
  - 删除旧 `predictions` 字段 + 顶层孤儿 `dividends_*/monthly_changes_*`
  - 顶层字段从 20 精简到 15
  - `generated` 更新为 2026-05-21
- **15:30任务新增**：对账单自动检测（新Excel则重解析）+ 月度K线补充 + `generated` 写入
- **08:30任务新增**：`generated` 字段写入
- **审计脚本**：scripts/audit_system.py（全字段时效性检查）
- **对账单策略**：用户手动提供，不走定时检测

## v0.1 备份 → v0.2 Watchlist 重构
- **备份**：backups/v0.1/ 完整快照
- **清理**：删除36个过期文件（debug脚本/旧数据/API dump），37→12核心文件
- **Watchlist**：data/watchlist.json 单一配置源，注入 system_data.json
- **HTML 动态化**：所有页面股票切换按钮从 DATA.watchlist 动态生成
- **Python 重构**：3个核心脚本全部遍历 watchlist
- **自动化更新**：08:30/15:30提示词改为读取 watchlist 循环
- **效果**：新增股票只需在 watchlist.json 加一行，全系统自动适配

## v0.3 本地服务器 + 页面操作能力
- **server.py**：Python stdlib HTTP服务器，端口8765，9个API端点
- **HTML新增第9页"⚙ 管理"**：加/删股票 + 系统操作 + 服务器状态
- **触发按钮**：新闻/预测/专家分析页各加刷新按钮
- **启动**：start_server.bat 一键启动
- **Bug修复**（第二轮）：
  - `event.target` 炸JS → showPage改用onclick属性匹配nav高亮
  - `DATA.predictions` 字段已删除但代码仍引用 → 改为季节数据驱动预测与方案页
  - 预测页旧Chart重复残留 → 清理
  - 服务器单线程阻塞 → ThreadingMixIn多线程
  - CDN 加载阻塞（jsdelivr 在国内被墙）→ 换 cdnjs + unpkg 双CDN
  - init() 被 CDN 阻塞不执行 → DOMContentLoaded + 2秒fallback机制，Chart不可用也渲染文本
  - renderNews 中 `names` 残留 → 改 getStockName

## 系统当前结构（9个页面）
持仓总览 | K线走势 | 交易记录 | 手续费分析 | 预测与方案 | 新闻动态 | 专家分析 | 日预测
