# 项目规则 - 股票新闻监控与分析系统

## 项目概述
基于 Python http.server 的股票监控系统，提供新闻抓取、K线分析、预测生成、持仓管理、专家报告等功能，数据存储于 SQLite，前端为单页 HTML 应用。

---

## 技术栈
- **运行时**: Python 3.14.3（.workbuddy 版本）、Node.js 22.12.0
- **Web 服务器**: `server.py` — 基于 `ThreadingMixIn` + `HTTPServer`，端口 8765
- **数据库**: SQLite — `data/stock.db`，WAL 模式，外键约束开启
- **外部数据源**: NeoData（westock-data Node.js 插件）
- **前端**: 单页 HTML — `deliverables/bank-stock-system.html`
- **关键路径**:
  - Python: `C:\Users\28312\.workbuddy\binaries\python\versions\3.14.3\python.exe`
  - Node.js: `C:\Users\28312\.workbuddy\binaries\node\versions\22.12.0\node.exe`
  - westock-data: `.workbuddy/plugins/.../westock-data`
- **标准库**（无第三方依赖）: `http.server`, `sqlite3`, `subprocess`, `threading`, `json`, `re`, `concurrent.futures`
- **额外库**（仅特定脚本）: `pandas`, `openpyxl`

---

## 目录结构
```
├── server.py                          # Flask 替代 — ThreadedHTTPServer，主入口
├── start_server.bat                   # 启动脚本
├── .gitignore
├── docs/specs/                        # 19 份规范文档
│   ├── 01-api-server.md              # Web API 服务层
│   ├── 02-database-layer.md          # 数据库访问层 (db_helper.py)
│   ├── 03-sync-engine.md            # 全模块同步引擎 (sync_all.py)
│   ├── 04-self-learning.md          # 自学习与预测算法
│   ├── 05-daily-update.md           # 每日更新（部分废弃）
│   ├── 06-scheduler.md              # 定时任务调度
│   ├── 07-news-fetcher.md           # 新闻抓取
│   ├── 08-stock-database.md         # 股票数据库构建
│   ├── 09-statement-parser.md       # 券商对账单解析
│   ├── 10-expert-report.md          # 专家报告导入
│   ├── 11-data-injection.md         # 数据注入（reinject）
│   ├── 12-migration-and-audit.md    # 数据迁移与审计
│   ├── appendix-a-schema.md         # 数据库 Schema（17 表）
│   ├── appendix-b-api.md            # API 端点清单
│   ├── appendix-c-config.md         # 系统配置项
│   ├── appendix-d-dependencies.md   # 依赖关系图
│   ├── appendix-e-glossary.md       # 术语表
│   └── appendix-f-known-issues.md   # 已知问题
├── data/
│   ├── stock.db                     # SQLite 数据库（17 表）
│   ├── a_stocks.json                # A 股全量列表（5000+ 条）
│   ├── broker_statement.json        # 券商对账单解析结果
│   ├── config.json                  # 系统配置
│   └── watchlist.json               # 自选股列表
├── deliverables/
│   ├── bank-stock-system.html       # 前端单页应用
│   └── chart.umd.min.js             # 图表库
└── scripts/
    ├── db_helper.py                 # 数据库访问层（CRUD 入口）
    ├── sync_all.py                  # 全模块同步引擎（8 步流程）
    ├── fetch_news.py                # 新闻抓取
    ├── daily_update.py              # 每日更新（部分废弃）
    ├── scheduler.py                 # 定时任务调度
    ├── update_from_statement.py     # 对账单解析生产版
    ├── parse_statement.py           # 对账单解析原始版
    ├── reinject_from_db.py          # 数据注入（从 SQLite → HTML）
    ├── reinject_data.py             # 数据注入旧版
    ├── import_expert_report.py      # 专家报告导入
    ├── build_stock_db.py            # 股票数据库构建
    ├── audit_system.py              # 系统审计
    ├── migrate_to_sqlite.py         # 数据迁移
    └── ...其他工具脚本
```

---

## 数据库 Schema（17 张表）
数据库文件：`data/stock.db`，WAL 模式，外键约束开启。

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `stocks` | A 股全量列表 | code(PK), name, market, py, watchlist |
| `watchlist` | 自选股 | code(PK), name, market, sort_order |
| `kline_daily` | 日K线 | code, date, open, close, high, low |
| `kline_monthly` | 月K线 | code, date, open, high, low, close, volume, change_pct |
| `quotes` | 行情报价 | code(PK), price, change, open, high, low, pe, pb, dy |
| `daily_predictions` | 每日预测 | id(PK), code, date, direction, confidence, high, low, advice, actual_*, dir_hit, range_hit |
| `prediction_hourly` | 分时预测 | id(PK), pred_id(FK), block, pred_*, direction, strength, hit |
| `prediction_signals` | 技术信号快照 | id(PK), pred_id(FK), name, value, direction, raw_value |
| `learning_params` | 学习参数 | code(PK), signal_weights(JSON), hourly_bias(JSON), seasonal_adj(JSON), confidence_beta(JSON) |
| `accuracy_stats` | 准确率统计 | code(PK), period(PK), dir_*, range_* |
| `trades` | 交易记录 | id, date, time, code, name, type, qty, price, commission, stamp_tax, settlement |
| `positions` | 当前持仓 | code(PK), name, qty, total_cost, avg_cost, realized_pnl |
| `closed_positions` | 已清仓 | code(PK), name, realized_pnl, dividends_total, total_commission/stamp_tax/other_fees |
| `dividends` | 分红记录 | id, code, date, amount, price |
| `news` | 新闻 | id, date, code, title, summary, source, sentiment(major/neutral/negative), major |
| `expert_reports` | 专家报告 | id, date, report_data(JSON) |
| `seasonal` | 季节因子 | code(PK), factors(JSON — [factor1..12]) |

**数据完整性**: 代码逻辑保证（非 Schema 外键约束）: kline_daily/quotes/daily_predictions 等表的 code 必须 ⊆ watchlist.code，删除自选股时级联清理 9 张表。

---

## API 架构
- **基路径**: `http://localhost:8765`
- **统一响应**: `{success: bool, data?, error?, message?, output?, count?}`
- **HTTP 方法**: GET（查询）、POST（创建/触发）、DELETE（删除）、OPTIONS（CORS）
- **CORS**: `Access-Control-Allow-Origin: *`
- **端点分类**:
  - 初始化: `GET /api/v2/init` — 前端加载全量数据
  - 自选股: `/api/v2/watchlist` — CRUD
  - 行情: `/api/v2/quotes` — 批量/单股票
  - 持仓: `/api/v2/positions`, `/api/v2/trades`, `/api/v2/dividends`
  - K线: `/api/v2/kline/daily`, `/api/v2/kline/monthly`
  - 预测: `/api/v2/predictions/daily`
  - 新闻: `/api/v2/news?filter=all|major|<code>`
  - 专家报告: `/api/v2/expert`, `POST /api/v2/expert/import`
  - 学习/准确率: `/api/v2/learning`, `/api/v2/accuracy`
  - 季节性: `/api/v2/seasonal`
  - 搜索: `/api/search/stocks?q=`
  - 触发器: `POST /api/trigger/news|predict|update_statement`
  - 上传: `POST /api/upload/statement` (multipart/form-data)
  - 审计: `GET /api/audit`
- **并发控制**: `threading.Lock` + `_refresh_in_progress`，并发同步返回 429
- **子进程编排**: 通过 `subprocess.run()` 调用后台脚本，sync_all 超时 180s，fetch_news 超时 60s

---

## 核心架构模式

### 1. 同步引擎（sync_all.py）— 8 步流程
1. 读取 watchlist
2. 新闻抓取
3. 并行日K线获取（ThreadPoolExecutor, max_workers=min(len, 4)）
4. 预测回填（验证历史预测准确率）
5. 准确率重算
6. 生成当日预测
7. 季节性 + 月K线补充 + 行情更新
8. 写遗留 system_data.json

### 2. 7 项技术指标（calc_signals）
MACD (EMA12/26/9), RSI (14日), Bollinger (20日2σ), KDJ (9日), Seasonal (12月), ATR (14日), Money Flow (3日+10日)

### 3. 预测生成（gen_pred）
加权分数 + 自学习参数 → 方向/置信度/区间/4时段的日内预测

### 4. 自学习引擎（daily_update.py Step5）
MWU 信号权重更新 + EG 偏置更新 + Beta-Binomial 置信度 + 季节因子 EMA

### 5. 数据注入（reinject_from_db.py）
将 SQLite 17 张表数据 → 正则替换入 HTML 的 `const DATA = {...}` 块

---

## 代码规范
- **DB 操作**: 全部通过 `db_helper.py`（参数化查询，`?` 占位符防注入）
- **子进程通信**: stdout 捕获，先 gbk 解码（Windows 兼容），fallback utf-8
- **日期格式**: `YYYY-MM-DD`
- **情感分析**: 关键词匹配（14 个正向 / 14 个负向关键词）
- **响应格式**: 所有端点 `try/except`，统一 JSON 响应

---

## 已知问题（详见 appendix-f-known-issues.md）
1. `daily_update.py` 与 `sync_all.py` 都写 `system_data.json`，可能互相覆盖
2. 行情数据 PE/PB/股息率硬编码为 0，无实时数据源
3. 数据注入依赖 HTML 特定格式（正则匹配 `const DATA = {...}`）
4. `server.py` (3.14.3) 与 `scheduler.py` (3.13.12) Python 版本不一致
5. 新闻无自动清理，长期运行数据库可能膨胀
6. 月K线无增量更新，需手动维护
7. 删除自选股时 JSON/SQLite 双写，可能不一致

---

## 性能基线
- 全量同步（3 股票）: 30-60s
- API 响应: < 100ms
- 数据库大小: ~2-5 MB
- 前端初始加载: ~300-500 KB

---

## 新增代码时需要注意
1. 新增 API 端点 → 同时更新 `appendix-b-api.md`
2. 新增/修改数据库 Schema → 同时更新 `appendix-a-schema.md`
3. 新增依赖 → 同时更新 `appendix-d-dependencies.md`
4. 使用 `db_helper.py` 的 CRUD 函数，不要直接 `sqlite3.connect`
5. 子进程 stdout 请用 gbk 解码 + utf-8 fallback
6. 编辑 `import_expert_report.py` 时注意 Schema 验证规则
7. 编辑 `update_from_statement.py` 时注意 `.bak` 备份逻辑
